{
  "name": "app-server",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/app-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/packages/app-server",
        "main": "packages/app-server/src/main.ts",
        "tsConfig": "packages/app-server/tsconfig.app.json",
        "assets": ["packages/app-server/src/assets"],
        "webpackConfig": "packages/app-server/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "app-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "app-server:build:development"
        },
        "production": {
          "buildTarget": "app-server:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/app-server/jest.config.ts"
      }
    }
  },
  "tags": []
}
