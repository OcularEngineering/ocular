{
  "name": "slack-bot",
  "version": "0.0.0",
  "main": "app.js",
  "description": "Slack Bot Application for Ocular",
  "author": "Louis Luxa Murerwa",
  "scripts": {
    "prepare": "cross-env NODE_ENV=production npm run build",
    "test": "jest --passWithNoTests src",
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist node_modules"
  },
  "dependencies": {
    "@ocular/ocular": "*",
    "@ocular/types": "*",
    "@ocular/utils": "*",
    "@slack/bolt": "^3.19.0"
  }
}
