import {createEntitiesInput} from "../types"

// import { joinerConfig } from "../joiner-config"




export default class CatalogService {
/*
  * Create a new entity
*/
async create(entity:createEntitiesInput): Promise<Entity>{

}
// /*
//   * List all entities in the catalog
// */
// async list(selector: Selector<FilterableUserProps> & { q?: string } = {},
//   config: FindConfig<FilterableUserProps> = { skip: 0, take: 50 }): Promise<Entity[]>{
//   // return this.catalogService.list(selector, config)
// }
// /*
//   * List all entities in the catalog and count them
// */
// listAndCount( selector: Selector<FilterableUserProps> & { q?: string } = {},
//   config: FindConfig<FilterableUserProps> = { skip: 0, take: 50 }){

// }
// /*
//   * Retrieve a specific entity
// */
// async retrive(userId: string, config: FindConfig<Entity> = {}): Promise<Entity>{

// }
// /*
//   * Delete an entity
// */
// async delete(entityId: string): Promise<void>{

// }

}