{
  "name": "core-backend",
  "version": "1.0.0",
  "description": "Description of your core-backend project",
  "main": "dist/app.js",
  "scripts": {
    "watch": "tsc --build --watch",
    "prepublishOnly": "cross-env NODE_ENV=production tsc --build",
    "build": "rimraf dist && tsc --build",
    "start": "npm run build && nodemon dist/packages/core-backend/src/main.js",
    "test": "jest --runInBand --bail --detectOpenHandles --forceExit",
    "typeorm": "npm run build && typeorm-ts-node-esm -d dist/packages/core-backend/src/data-source.js"
  },
  "keywords": [
    "core",
    "backend",
    "nx"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@azure/search-documents": "^12.0.0",
    "@pinecone-database/pinecone": "^2.0.1",
    "algoliasearch": "^4.22.1",
    "passport-custom": "^1.1.1",
    "ts-node": "^10.9.2",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
