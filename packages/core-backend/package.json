{
  "name": "core-backend",
  "version": "1.0.0",
  "description": "Description of your core-backend project",
  "main": "dist/app.js",
  "scripts": {
    "watch": "tsc --build --watch",
    "prepublishOnly": "cross-env NODE_ENV=production tsc --build",
    "build": "rimraf dist && tsc --build",
    "start": "nodemon dist/packages/core-backend/src/main.js",
    "test": "jest --runInBand --bail --detectOpenHandles --forceExit"
  },
  "keywords": [
    "core",
    "backend",
    "nx"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "passport-custom": "^1.1.1",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
